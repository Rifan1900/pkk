document,addEventListener("DOMContentLoaded",function()) 
    const takslist = document.getElementByid("tasklist");
    const newtaksinfut = document.getElementById("new-task");
    const addEventListener = document.getElementById("add-task")

    addtaskbuttom,addEventListener("click, funtion") () {
        const tesktext = newtaksinfut.value,trim();
        if (tasktext !=="") {
            addtask(tasktext);
            new taskinfut.value ="";
        }




    }
